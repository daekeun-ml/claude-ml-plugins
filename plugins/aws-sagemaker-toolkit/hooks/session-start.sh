#!/usr/bin/env bash
# Emit this plugin's rules as SessionStart additionalContext.
# (SessionStart does not support prompt-type hooks; command-type + JSON stdout is the supported path.)
cat <<'JSON'
{
  "hookSpecificOutput": {
    "hookEventName": "SessionStart",
    "additionalContext": "[aws-sagemaker-toolkit rules, always apply to AWS ML infra work]\n\nTOOL-CALL ENCODING (hard rule): always write Korean and other non-ASCII strings in tool-call parameters as literal UTF-8; never as \\uXXXX unicode escapes. Hand-spelled hex codepoints produce near-miss syllables that silently corrupt Hangul (e.g. 완료 rendered as 완릷); see anthropics/claude-code#83033. Applies to every tool parameter, especially AskUserQuestion labels, task titles, and file contents.\n\nKOREAN PROSE STYLE (anti-slop): no em dash (—): use commas, parentheses, a colon, or split the sentence; also avoid -- and en dash as substitutes. Do not use the middle dot (·) to list keywords: write 'A, B, C' or 'A와 B' instead. Mix 해요체 and 습니다체 rather than repeating one ending; default about 5:5 for blog/guide prose, adjust by context (more 습니다체 for formal docs and release notes, more 해요체 for tutorials and conversational replies), never drop to 반말/음슴체. Bullets, rule statements, and table cells are exempt from the ratio. Skip filler openers like '~의 핵심은' and '결론적으로 말하면'.\n\nRouting: prose/guides/decisions to aws-solutions-architect; lab code/notebooks to aws-ml-engineer; E2E finetune assets to sagemaker-finetune-engineer; agentic code to agentic-integration-engineer; fact verification to aws-fact-checker (read-only). Author and verify lanes stay separate; never self-approve your own facts.\n\nTier accuracy (no misattribution): EC2 (self) / HyperPod (semi, Slurm and EKS) / SageMaker (fully). Known traps: SageMaker deployment guardrails (blue/green, canary) are SageMaker-classic only, NOT HyperPod; Serverless Inference has NO GPU (as of now); Batch Transform is a job, not an endpoint; DLC (workload container) is not DLAMI (node host image), and DLC is not managed-only; ParallelCluster DOES auto-replace nodes (the HyperPod differentiator is control-plane ownership, deep health checks, and auto-resume); SageMaker endpoint (boto3 sagemaker-runtime invoke_endpoint) is not Bedrock (bedrock-runtime Converse), separate clients; Bedrock Claude needs an inference-profile-prefixed model id (us./eu./apac./global.), never hardcode a static model roster.\n\nFacts: never write a guessed fact as confirmed; mark fast-changing values (region, GA, limits, pricing, model IDs, agent SDK APIs) as 'as of now, re-verify'; unverified code API gets '# TODO verify'. All guides open with TL;DR (one line), then existing Pain Point, then Why. Attach both docs.aws.amazon.com and official GitHub links in sources. Lab code: tier-idiom no-mixing, placeholders not secrets, mandatory cleanup cell (avoid GPU/endpoint billing), CloudWatch direct links. Language: Korean prose, English for service/API/identifier names; notebooks beginner-friendly, scripts precise.\n\nFull rule text ships in this plugin's rules/ directory (aws-authoring, aws-handson-testing, code-style, communication, fact-integrity, sagemaker-e2e). See the plugin README for enabling them as always-on @import rules in your own CLAUDE.md."
  }
}
JSON
exit 0
